# Build dependencies (must be first)
setuptools==65.5.1
wheel==0.38.4

# Core dependencies
numpy==1.23.5

# Pinecone with gRPC support (includes inference API)
pinecone[grpc]

# Web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0

# Rate limiting
slowapi==0.1.9

# Utilities
requests==2.31.0
python-dotenv==1.0.0

# Cache
upstash-redis

# Monitoring
psutil

# UI (optional - not needed for API deployment)
# gradio==4.44.0